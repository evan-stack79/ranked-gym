/**
 * CODEX-RISK — PR-I (next wave).
 *
 * Do not implement export/import/verify/reconcile here.
 * Runtime env names only (never commit values):
 * MIGRATION_SUPABASE_URL, MIGRATION_SUPABASE_SERVICE_ROLE_KEY,
 * MIGRATION_CONVEX_URL, MIGRATION_CONVEX_ADMIN_KEY, MIGRATION_RUN_ID
 */
export const CODEX_RISK_MIGRATION_SCRIPTS_PENDING = true
