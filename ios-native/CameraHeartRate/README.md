# CameraHeartRate (iOS)

Source of truth for the Capacitor plugin:

`ios/App/App/Plugins/CameraHeartRatePlugin.swift`

That file is a member of the **App** Xcode target (Compile Sources).
Do not keep a second Swift copy here — it would not be compiled.
