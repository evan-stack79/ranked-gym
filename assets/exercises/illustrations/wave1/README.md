# Wave 1 — illustrations d’exercices (hors UI)

Série 3D monochrome graphite, **non branchée** à l’application.

- Pas de changement dans `src/utils/exerciseMedia.ts` ni le sélecteur.
- L’asset validé `src/assets/exercises/developpe-couche.webp` n’est **pas** remplacé.
- Un exercice inconnu continue d’utiliser le fallback neutre existant.
- Mapping canonique → fichier : `manifest.json` uniquement (pas de déduction depuis un nom libre).

**STOP** : attendre le GO Evan avant toute intégration Ranked Gym.
